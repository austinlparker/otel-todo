ingress:
  enabled: true
  staticIpResourceName: oteltodo-ip
  staticIp: 34.120.230.249
image:
  repository: docker.io/aparker
  tag: latest
  pullPolicy: IfNotPresent
client:
  name: todo-client
  serviceType: LoadBalancer
  targetPort: 80
server:
  name: todo-server
  serviceType: LoadBalancer
  targetPort: 9000
mysql-server:
  mysqlUser: todouser
  mysqlPassword: todopw
  mysqlDatabase: todos
# if running locally, set this value here or on the helm command line
lightstepKey: 